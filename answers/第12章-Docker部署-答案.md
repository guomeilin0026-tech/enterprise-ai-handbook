# 第 12 章：Docker 部署参考答案

## Docker 解决什么问题

参考答案：

```text
Docker 把应用和运行环境打包，减少“我电脑能跑、服务器不能跑”的问题，方便部署前端、后端、数据库、向量库等多个服务。
```

## docker-compose 适合什么

参考答案：

```text
企业 AI 项目通常包含多个服务，例如前端、后端、PostgreSQL、Qdrant、Redis、Nginx。docker-compose 可以用一个配置文件统一启动这些服务。
```

