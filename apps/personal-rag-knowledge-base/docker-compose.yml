services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - rag_data:/app/data
    env_file:
      - .env

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  rag_data:

